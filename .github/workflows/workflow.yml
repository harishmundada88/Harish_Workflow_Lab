name: My-Firdt-Github-Workflow

run-name: ${{github.actor}} is learning Github Actions

on: [push]

jobs:
  my-custom-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: echo message
        run: echo "Hellow World"
      - name: display hostname
        run: hostname